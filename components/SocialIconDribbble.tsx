import React from 'react';
import { Dribbble } from 'react-feather';
import { SocialIcon } from './SocialIcon';

export let SocialIconDribbble: React.FC = (props) => {
  return <SocialIcon icon={Dribbble} {...props}></SocialIcon>;
};
